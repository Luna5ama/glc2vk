# Vibris NeoForge adapter skeleton

This directory is intentionally excluded from `settings.gradle.kts`. It reserves the loader adapter boundary for a future NeoForge implementation; the current release supports Fabric 1.21.11 only.
